var counter = 5; // Начальное значение переменной counter
	while (counter < 10) { 
		counter++; // переменная counter + 1
		document.write("Counter " + counter); // Выводит на экран полученный результат
		break; // Прерывает выполнение текущего цикла
		document.write("Эта строчка не выполняется."); // Эта строчка не выполняется
	}
	// Программа выведет значение "Counter 6"     